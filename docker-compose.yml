services:

  app:
    build: .
    container_name: nest-app
    restart: always
    env_file:
      - .env
    ports:
      - '3333:3333'
    command: npx prisma migrate deploy && yarn start:prod
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

