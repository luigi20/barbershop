export type IdAndName = {
  id: string;
  name: string;
};

export type IdAndNameAndOwnerBarbershop = {
  id: string;
  name: string;
  owner_id: string;
};

export type InfoService = {
  duration: number;
  price: number;
  service_id: string;
};
